const ACTIONS = {
    JOIN: 'join',
    JOINED: 'joined',
    DISCONNECTED: 'disconnected',
    GAME_CREATED: 'game_created',
    JOIN_REQUEST:'join-request'
}
const ROOMIDs = {
    GAME_LIST:'game-list',
}
export {ACTIONS,ROOMIDs}
